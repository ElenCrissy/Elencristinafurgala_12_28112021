import styled from "styled-components";
import {
    ResponsiveContainer,
    AreaChart,
    XAxis,
    YAxis,
    Area,
    Tooltip,
    CartesianGrid
} from "recharts";

function DailyActivities(){
    return(
        null
    )
}

export default DailyActivities